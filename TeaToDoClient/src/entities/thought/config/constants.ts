import { API_URL } from "@/shared/utils/constants";

export const THOUGHT_ENDPOINT = `${API_URL}/thoughts`;
export const THOUGHT_EMOJI_ENDPOINT = `${API_URL}/thoughts/emoji`;
export const THOUGHT_TEXT_ENDPOINT = `${API_URL}/thoughts/text`;