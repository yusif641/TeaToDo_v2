import { API_URL } from "@/shared/utils/constants";

export const TASK_ENDPOINT = `${API_URL}/tasks`;
export const TASK_STATE_ENDPOINT = `${API_URL}/tasks/state`;
export const TASK_TEXT_ENDPOINT = `${API_URL}/tasks/text`;