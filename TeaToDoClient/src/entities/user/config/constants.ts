import { API_URL } from "@/shared/utils/constants";

export const USER_INFO_ENDPOINT = `${API_URL}/user`;
export const LOGOUT_ENDPOINT = `${API_URL}/auth/logout`;
export const UPDATE_USER_NICKNAME = `${API_URL}/user/nickname`;
export const USER_AVATAR = `${API_URL}/user/avatar`;